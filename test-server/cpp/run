#!/bin/bash

. $(dirname $0)/config.sh

cp -r $(dirname $0)/../test_images $(dirname $0)

$DOCKER_RUN -p 50051:50051 --name $CONTAINER_NAME -d $DOCKER_IMAGE $(dirname $0)/server
echo "Remember to: docker kill $CONTAINER_NAME"